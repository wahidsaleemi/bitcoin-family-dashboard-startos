# TODO

- [ ] Community submission: verify all workflows fire correctly once the repo
      is forked into Start9-Community (build on PR, tag+release on merge to
      master, publish to community-beta).
- [ ] Add `aarch64` build verification (currently only `x86_64` is built/tested
      on the dev box).
- [ ] Confirm the watch-only balance scan fully completes on a box without
      Bitcoin Core once public API rate limits clear (background retry should
      land it; verify end-to-end).
